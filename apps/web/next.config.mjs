/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    appDir: true
  },
  styledComponents: false,
  compiler: {
    styledComponents: false
  }
};

export default nextConfig;
